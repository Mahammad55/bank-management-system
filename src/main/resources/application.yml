server:
  port: 9090

  servlet:
    context-path: /bank

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bankdemo
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false